server:
  port: 8081
spring:
  application:
    name: backend-config-service
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/Aaldeao/Preu_TopEducation_Microservicio
          search-paths: config-data